<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>MediConnect — My Appointments</title>
  <style>
    /* ===== BODY ===== */
    body {
      font-family: "Poppins", sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      background: url("/images/background.jpeg") no-repeat center center;
      background-size: cover;
      display: flex;
      flex-direction: column;
    }

    /* ===== HEADER ===== */
    .navbar {
      position: sticky;
      top: 0;
      background: #2a9df4;
      color: #fff;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.8rem 2rem;
      box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
      z-index: 10;
    }

    .navbar .logo span {
      font-weight: 600;
      margin-left: 0.5rem;
    }

    .nav-links a {
      color: #fff;
      text-decoration: none;
      margin-left: 1rem;
      transition: 0.3s;
    }

    .nav-links a:hover,
    .nav-links a.active {
      text-decoration: underline;
    }

    /* ===== MAIN CONTAINER ===== */
    .container.appointment-page {
      flex: 1;
      padding: 2rem 1rem;
      max-width: 900px;
      margin: 0 auto;
    }

    /* ===== PAGE TITLE ===== */
    .appointment-page h1 {
      color: black;
      margin-bottom: 1.5rem;
      text-align: center;
    }

    /* ===== CONTROLS ===== */
    .appointments-controls {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
      margin-bottom: 2rem;
    }

    .appointments-controls input,
    .appointments-controls select {
      padding: 0.6rem 1rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      outline: none;
      background-color:rgba(255,255,255, 0.4);
      width: 200px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .appointments-controls input:focus,
    .appointments-controls select:focus,
    .appointments-controls input:hover,
    .appointments-controls select:hover {
      transform: scale(1.05);
      box-shadow: 0 6px 20px rgba(0,0,0,0.4);
    }

    /* ===== APPOINTMENTS LIST ===== */
    .appointments-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .appointment-card {
      background: rgba(255, 255, 255, 0.1);
      padding: 1rem 1.5rem;
      border-radius: 1rem;
      color:rgba(0,0,0, 1.2) ;
      box-shadow: 0 4px 15px rgba(0,0,0,0.5);
      display: flex;
      justify-content: space-between;
      align-items: center;
      backdrop-filter: blur(8px);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .appointment-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.5);
    }

    .appointment-info {
      display: flex;
      flex-direction: column;
    }

    .appointment-info span {
      margin-bottom: 0.2rem;
    }

    .appointment-status {
      font-weight: 600;
      padding: 0.3rem 0.8rem;
      border-radius: 20px;
      text-transform: uppercase;
    }

    .appointment-status.Pending { background: #2a9df4;font-weight: 600; color: #fff; }
    .appointment-status.Confirmed { background:  #2a9df4;font-weight: 600;color: #fff; }
    .appointment-status.Cancelled { background:  #2a9df4; font-weight: 600;color: #fff; }

    /* ===== FOOTER ===== */
    .site-footer {
      text-align: center;
      padding: 2rem 1rem;
      background: #2a9df4;
      color: #fff;
      border-top-left-radius: 1rem;
      border-top-right-radius: 1rem;
      margin-top: auto;
      box-shadow: 0 -4px 15px rgba(0,0,0,0.3);
    }

    .site-footer p {
      margin: 0;
      font-size: 0.95rem;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 600px) {
      .appointments-controls {
        flex-direction: column;
        align-items: center;
      }

      .appointments-controls input,
      .appointments-controls select {
        width: 100%;
        max-width: 300px;
      }

      .appointment-card {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>
</head>
<body>
  <header class="navbar">
    <div class="nav-left"><div class="logo">🩺 <span>MediConnect</span></div></div>
    <div class="nav-right">
      <button id="themeToggle" hidden class="icon-btn" title="Toggle theme">🌗</button>
      <button id="menuToggle" hidden class="icon-btn mobile-only">☰</button>
      <nav id="navLinks" class="nav-links">
        <a href="/user/home">Home</a>
        <a href="/user/doctors">Doctors</a>
        <a href="/user/bookapp">Book</a>
        <a href="/user/appointments" class="active">My Appointments</a>
      </nav>
    </div>
  </header>

  <main class="container appointment-page">
    <h1>My Appointments</h1>

    <div id="appointmentsList" class="appointments-list">
      <!-- Sample Appointment Cards -->
    <%appoints.forEach(appi=>{%>
      <div class="appointment-card">
        <div class="appointment-info">
          <span><strong>Doctor:</strong><%= appi.doctor%></span>
          <span><strong>Date:</strong><%= new Date(appi.date).toISOString().split("T")[0]%></span>
          <span><strong>Time:</strong><%= appi.time%></span>
        </div>
        <div class="appointment-status Pending"><%= appi.Status%></div>
     </div>
    <% })%>

  </main>

  <footer class="site-footer">
    <div class="container"><p>© 2025 MediConnect</p></div>
  </footer>

  <div id="toasts"></div>
</body>
</html>
