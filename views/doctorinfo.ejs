<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>MediConnect — Doctors</title>
<style>
body {
  font-family: "Poppins", sans-serif;
  margin:0;
  padding:0;
  background: #f6f9fc;
  color:#222;
  background: 
    linear-gradient(rgba(255,255,255,0.1), rgba(255,255,255,0.1)),
    url("/images/background.jpeg");
  background-size: cover;
  background-position: center;
}

/* Add this below the body style */
html, body {
  height: 100%;
  display: flex;
  flex-direction: column;
}

main {
  flex: 1;
}

/* ===== HEADER ===== */
.navbar {
  position: sticky;
  top:0;
  background:#2a9df4;
  color:#fff;
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:0.8rem 2rem;
  box-shadow:0 3px 10px rgba(0,0,0,0.1);
  z-index:10;
}
.navbar .logo span { font-weight:600; margin-left:0.5rem; }
.nav-links a { color:#fff; text-decoration:none; margin-left:1rem; transition:0.3s; }
.nav-links a:hover, .nav-links a.active { text-decoration:underline; }

/* ===== PAGE HEADER ===== */
.page-header { text-align:center; margin:2rem 1rem 1rem; }
.page-header h1 { font-size:1.9rem; margin-bottom:1rem; color:#2a9df4; }

/* ===== DOCTORS GRID ===== */
.doctors-grid {
  display:grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap:1rem;
  padding:1rem 2rem 2rem;
}
.doctor-card {
  background:rgba(0,0,0,0.4);
  border-radius:12px;
  box-shadow:0 6px 15px rgba(0,0,0,0.1);
  text-align:center;
  padding:1rem;
  transition:0.3s;
}
.doctor-card:hover { transform:translateY(-4px); box-shadow:0 8px 20px rgba(0,0,0,0.15); }
.doctor-card img {
  width:100px;
  height:100px;
  border-radius:50%;
  object-fit:cover;
  margin-bottom:0.75rem;
}
.doctor-card h3 { margin:0.5rem 0 0.3rem; color:#2a9df4; font-size:1.2rem; }
.doctor-card p { margin:0.25rem 0; font-size:0.9rem; color:#2a9df4 ;font-size:1.1rem}
.doctor-card .fee { font-weight:600; color:#111; margin-top:0.5rem; }

/* ===== FOOTER ===== */
.site-footer {
  text-align:center;
  padding:1.5rem 1rem;
  background:#2a9df4;
  color:#fff;
  box-shadow:0 -4px 15px rgba(0,0,0,0.12);
}
.site-footer p { margin:0; font-size:0.95rem; }

/* Responsive */
@media(max-width:600px){ .doctors-grid{grid-template-columns:1fr;} }
</style>
</head>
<body>

<header class="navbar">
  <div class="nav-left"><div class="logo">🩺 <span>MediConnect</span></div></div>
  <div class="nav-right">
    <nav class="nav-links">
      <a href="/home">Home</a>
      <a href="/user/doctors" class="active">Doctors</a>
      <a href="/user/bookapp">Book</a>
      <a href="/user/appointments">My Appointments</a>
    </nav>
  </div>
</header>

<main class="container doctors-page">
  <div class="page-header">
    <h1>Find a Doctor</h1>
  </div>

  <div class="doctors-grid">
    <% doctors.forEach(doc=>{%>
    <div class="doctor-card">
      <img src="/images/download.jpg" alt="Dr. Ahmed">
      <h3><%= doc.d_name%></h3>
      <p><strong>Specialty:</strong><%= doc.d_specialization%></p>
      <p class="fee">Fee: PKR <%= doc.d_fees %></p>
    </div>
    <%})%>
  </div>
</main>

<footer class="site-footer">
  <div class="container"><p>© 2025 MediConnect</p></div>
</footer>

</body>
</html>
